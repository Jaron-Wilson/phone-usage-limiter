<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Only for the web panels you put on a screen edge. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature android:name="android.hardware.nfc.hce" android:required="false" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <!-- Only ever used to switch a calendar's own sync on or off, never to
         create, change or delete an event. -->
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <!-- Optional. Only to notice when you are at a place you saved, so it can
         switch the mode. Background access lets that work while the app is not
         open; without it, places only update while Modes is on screen. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <!-- The wake alarm is ours: it rings over the lock screen with a full-screen
         alarm, which needs this. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- The alarm rings from a foreground service so the sound never depends on
         a screen taking over. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- To repair a calendar account that Android reports as enabled while
         refusing to sync it. Both are install-time permissions. -->
    <uses-permission android:name="android.permission.READ_SYNC_SETTINGS" />
    <uses-permission android:name="android.permission.WRITE_SYNC_SETTINGS" />
    <uses-permission android:name="android.permission.READ_SYNC_STATS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />
    <!-- Optional. Screen time per app on the Stats screen. Granted by hand in
         Settings > Usage access, never at install. -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- A launcher has to be able to list what is installed, including apps
         that have been archived and so have no launcher entry of their own.
         The <queries> block below is not enough for that. This app is
         sideloaded, so Play's restrictions on this permission do not apply. -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:name=".ModesApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Modes"
        tools:targetApi="35">

        <!-- Settings / dashboard -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Modes">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Minimal home screen. Opt-in: only active if set as default launcher. -->
        <activity
            android:name=".launcher.LauncherActivity"
            android:exported="true"
            android:label="@string/launcher_name"
            android:launchMode="singleTask"
            android:stateNotNeeded="true"
            android:excludeFromRecents="true"
            android:taskAffinity="dev.jaronwilson.modes.home"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Modes.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Speed bump shown when a blocked app comes to the foreground -->
        <activity
            android:name=".guard.InterstitialActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:taskAffinity="dev.jaronwilson.modes.guard"
            android:theme="@style/Theme.Modes" />

        <!-- Reads every notification, decides what reaches you -->
        <service
            android:name=".notify.NotificationGate"
            android:exported="true"
            android:label="@string/gate_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
            <meta-data
                android:name="android.service.notification.default_filter_types"
                android:value="conversations|alerting|silent" />
        </service>

        <!-- Optional: bounces you out of apps not allowed in the current mode -->
        <service
            android:name=".guard.AppGuardService"
            android:exported="true"
            android:label="@string/guard_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <receiver
            android:name=".schedule.ModeAlarmReceiver"
            android:exported="false" />

        <!-- The screensaver: the nearest thing to an always-on display that an
             app is allowed to provide. -->
        <service
            android:name=".dream.ModesDream"
            android:exported="true"
            android:label="@string/dream_name"
            android:permission="android.permission.BIND_DREAM_SERVICE">
            <intent-filter>
                <action android:name="android.service.dreams.DreamService" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <meta-data
                android:name="android.service.dream"
                android:resource="@xml/dream_info" />
        </service>

        <!-- Presents one link as an NFC tag when another phone taps this one. -->
        <service
            android:name=".nfc.TapCardService"
            android:exported="true"
            android:permission="android.permission.BIND_NFC_SERVICE">
            <intent-filter>
                <action android:name="android.nfc.cardemulation.action.HOST_APDU_SERVICE" />
            </intent-filter>
            <meta-data
                android:name="android.nfc.cardemulation.host_apdu_service"
                android:resource="@xml/apduservice" />
        </service>

        <receiver
            android:name=".commute.LeaveReceiver"
            android:exported="false" />

        <receiver
            android:name=".commute.WorkAlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".alarm.WakeAlarmReceiver"
            android:exported="false" />

        <activity
            android:name=".alarm.AlarmActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.Modes" />

        <service
            android:name=".alarm.AlarmService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="An alarm ringing until the user dismisses it" />
        </service>

        <receiver
            android:name=".notify.DigestActionReceiver"
            android:exported="false" />

        <!-- Lets the share sheet read the exported dump. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Triggered over adb by tools/pull-apps.sh. Writes a list of installed
             apps to this app's own files directory and nothing else. -->
        <receiver
            android:name=".tools.ExportReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="dev.jaronwilson.modes.EXPORT_APPS" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".schedule.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".schedule.CalendarChangedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.PROVIDER_CHANGED" />
                <data android:scheme="content" android:host="com.android.calendar" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
